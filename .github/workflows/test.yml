name: Temporary profile repair
on:
  push:
    paths:
      - '.github/workflows/test.yml'
permissions:
  contents: write
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo profile repair
